sudo: required
services:
  - docker


before_install:
  - docker build -t nachomiquel/docker-react -f Dockerfile.dev . 

script:
  - docker run nachomiquel/docker-react npm run test -- --coverage

deploy:
  - provider: elasticbeanstalk
